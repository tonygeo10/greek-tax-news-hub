{
  "name": "greek-tax-news-backend",
  "version": "1.0.0",
  "description": "Backend API for Greek Tax News Hub",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup-db": "node -e \"require('mssql').connect(require('./db-config.js')).then(pool => { console.log('Database connected'); process.exit(0); }).catch(err => { console.error('Connection failed:', err.message); process.exit(1); })\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mssql": "^9.1.1",
    "cors": "^2.8.5",
    "rss-parser": "^3.13.0",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
